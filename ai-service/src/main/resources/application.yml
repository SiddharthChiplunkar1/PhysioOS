server:
  port: 8103

spring:
  application:
    name: ai-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_db
    username: ai_db
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
