server:
  port: 8096

spring:
  application:
    name: crm-service
  datasource:
    url: jdbc:postgresql://localhost:5432/crm_db
    username: crm_db
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
