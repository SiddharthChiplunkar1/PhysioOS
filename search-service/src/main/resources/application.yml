server:
  port: 8106

spring:
  application:
    name: search-service
  datasource:
    url: jdbc:postgresql://localhost:5432/search_db
    username: search_db
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
