server:
  port: 8083

spring:
  application:
    name: authorization-service
  datasource:
    url: jdbc:postgresql://localhost:5432/authorization_db
    username: authorization_db
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
