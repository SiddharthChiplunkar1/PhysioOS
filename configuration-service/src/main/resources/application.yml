server:
  port: 8094

spring:
  application:
    name: configuration-service
  datasource:
    url: jdbc:postgresql://localhost:5432/configuration_db
    username: configuration_db
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
