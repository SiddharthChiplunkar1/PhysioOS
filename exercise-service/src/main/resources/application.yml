server:
  port: 8089

spring:
  application:
    name: exercise-service
  datasource:
    url: jdbc:postgresql://localhost:5432/exercise_db
    username: exercise_db
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
