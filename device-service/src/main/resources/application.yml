server:
  port: 8091

spring:
  application:
    name: device-service
  datasource:
    url: jdbc:postgresql://localhost:5432/device_db
    username: device_db
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
